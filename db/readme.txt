the GeekStream.Core.dll is copied here as a post build event of the Geekstream.Core project.

The GeekStream folder contains the livedb database files.

The Server folder contains the LiveDB Server 0.1.0 beta binaries