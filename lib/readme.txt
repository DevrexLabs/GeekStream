The LiveDomain.Enterprise.Client lib included here is an unreleased intermediate development version. 

The lib supports transparently switching between embedded and remote engine which is useful for debugging the domain. The feature is being redesigned and will be included in release 0.3.0 of LiveDomain.Enterprise.Client.